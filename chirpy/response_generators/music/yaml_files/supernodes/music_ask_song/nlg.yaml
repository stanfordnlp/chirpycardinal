nlg:
  - node_name: cur_song_ent_exists
    required_flags:
      cur_song_ent_exists: True
    response: "{cur_song_ent_exists_response(flags['rg'])}"
  - node_name: song_slots_exists
    required_flags:
      song_slots_exists: True
    response: "{song_slots_exists_response(flags['rg'])}"
  - node_name: thats
    required_flags:
      thats: True
    response: "{thats_response(flags['rg'])}"
  - node_name: didnt_know
    required_flags:
      didnt_know: True
    response: "{didnt_know_response(flags['rg'])}"
  - node_name: answered_no
    required_flags:
      answered_no: True
    response: "{no_response(flags['rg'])}"
  - node_name: answered_yes
    required_flags:
      answered_yes: True
    response: "{yes_response(flags['rg'])}"
  - node_name: question
    required_flags:
      question: True
    response: "{question_response(flags['rg'])}"
  - node_name: opinion
    required_flags:
      opinion: True
    response: "{opinion_response(flags['rg'])}"
  - node_name: til_only
    required_flags:
      til_only: True
    response: "{til_response(flags['rg'])}"
  - node_name: catch_all
    required_flags:
      catch_all: True
    response: "{catch_all_response(flags['rg'])}"

